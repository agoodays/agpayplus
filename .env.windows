# ========================================
# AgPay+ Windows 环境配置文件
# ========================================

# ========== 基础配置 ==========
# 服务器IP或域名（用于CORS和JWT配置）
IPORDOMAIN=localhost

# ========== MySQL 配置 ==========
# MySQL 部署方式：
#   1. 宿主机 MySQL（默认）：Windows Docker Desktop使用 host.docker.internal 连接已安装的 MySQL
#   2. Docker MySQL：使用 db 服务名连接容器化 MySQL
# MySQL 服务器地址
# 方式 1：使用宿主机 MySQL（需在宿主机安装 MySQL 8.0+）
MYSQL_SERVER_NAME=host.docker.internal
# 方式 2：使用 Docker MySQL（取消注释下面一行）
# MYSQL_SERVER_NAME=db

MYSQL_PORT=3306
MYSQL_DATABASE=agpayplusdb
MYSQL_USER=root
MYSQL_PASSWORD=mysql*

# ========== 数据持久化路径 ==========
# Windows 路径格式
DATA_PATH_HOST=E:/app/agpayplus

# ========== Seq 日志服务器 ==========
# 如果使用内置 Seq 服务，默认 URL 为 http://seq:80
SEQ_URL=http://seq:80
ENABLE_SEQ=true
# 如果启用了 API Key，请在此填入（可留空）
SEQ_API_KEY=

# ========== RabbitMQ / MQ 配置 ==========
MQ_VENDER=RabbitMQ
MQ_HOSTNAME=rabbitmq
MQ_USERNAME=admin
MQ_PASSWORD=admin
MQ_PORT=5672

# ========== SSL 证书配置（宿主与容器路径/密码） ==========
# 主机上证书目录（用于 volumes 挂载）
# Windows 用户证书路径（替换 <YourUsername> 为实际用户名）
# 或使用: C:\Users\<YourUsername>\.aspnet\https
# CERT_PATH=C:\Users\${env:USERNAME}\.aspnet\https
CERT_PATH=${USERPROFILE}/.aspnet/https
# 容器内证书路径（docker-compose 中使用）
CERT_PATH_IN_CONTAINER=/https/agpayplusapi.pfx
# 证书密码
CERT_PASSWORD=123456

# ========== Docker Compose 项目名称 ==========
COMPOSE_PROJECT_NAME=agpayplus
